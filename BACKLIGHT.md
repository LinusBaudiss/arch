# Toggle backlight with xbacklight
---
Add `Option "Backlight" "intel_backlight"` in the Device Section to xorg.conf
Now the following to commands are avaiable
* `xbacklight -dec *number*`
* `xbacklight -inc *number*`
